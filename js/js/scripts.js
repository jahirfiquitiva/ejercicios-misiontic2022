const names = ['Jahir', 'Pablo', 'Laura', 'Camila'];
// forEach
// map
// filter
// some
// every

const numbers = [ 2, 4 , 5, 7, 8, 9]
const every = numbers.every((item) => {
  return item > 0
})

console.log(every);